Increment (++) Operator increases the value by 1
Decrement (--) Operator decreases the value by 1
(i) iterator. Container used for our counting 
let a = 1
console.log(--a);// a value is now 0
console.log(a); // this is now 0 because we decreased the variable's value